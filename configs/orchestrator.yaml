# Orchestrator Configuration
# Auto-generated by generate_configs.sh

host: "apt145.apt.emulab.net"
client_socket_port: 9000
server_socket_port: 9000

orchestrator_host: "apt145.apt.emulab.net"
orchestrator_port: 9000

# Cluster settings
replication_factor: 3
number_of_keys_hashtable: 10000
size_local_buffer: 1024
event_queue_size: 1000
worker_pool_size: 8
vnodes_number: 1

num_layers: 32

# Transfer backend selection
# use_gpu: true  -> GPUDirect RDMA (RDMAConnector, requires nvidia-peermem + A100-class GPU)
# use_gpu: false -> Standard RDMA via MooncakeConnector (no GPU required)
use_gpu: true

# Orchestrator specific settings
orchestrator:
  expected_decode_nodes: 1
  expected_prefill_nodes: 1

# Node configuration
node:
  role: "orchestrator"

# Prefill nodes list
prefill_nodes:
  - host: "apt142.apt.emulab.net"
    client_socket_port: 18500
    server_socket_port: 18501

# Decode nodes list
decode_nodes:
  - host: "apt158.apt.emulab.net"
    client_socket_port: 18600
    server_socket_port: 18601

# All peer nodes
peers:
  - host: "apt142.apt.emulab.net"
    client_socket_port: 18500
    server_socket_port: 18501
  - host: "apt158.apt.emulab.net"
    client_socket_port: 18600
    server_socket_port: 18601
