# Core dependencies
torch>=2.0.0
pyyaml>=6.0
requests>=2.28.0
aiohttp>=3.8.0

# vLLM (optional, only needed if running vLLM integration)
# vllm>=0.6.0

# pybind11 for C++ bindings
pybind11>=2.10.0

# Optional: FastAPI for REST API
# fastapi>=0.100.0
# uvicorn>=0.23.0
